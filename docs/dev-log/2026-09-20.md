# 开发日志 · 2026-09-20

## 今日完成

### 1. Pagefind 搜索修复
- 修复搜索 API 误用：`create()` → 正确用法 `init()` + `search()`
- 修复 `__VITE_PRELOAD__ is not defined`：`<script>` 改为 `<script is:inline>` 纯 JS，绕过 Vite 打包
- 重新 build + 索引生成，搜索功能正常
- 两个 bug 已记入 ISSUES_LOG（ISSUES-005、ISSUES-006）

### 2. 性能优化 + a11y 检查
- **移动端汉堡菜单**：之前手机端完全看不到导航，新增汉堡按钮 + 下拉菜单
- **sticky 顶栏**：导航栏滚动固定 + 毛玻璃效果
- **Skip to content**：键盘用户按 Tab 可跳过导航直接到正文
- **favicon**：蓝色 A 字母 SVG 图标
- **SEO meta**：Open Graph + Twitter Card + canonical + theme-color
- **prefers-reduced-motion**：系统开启减少动画时自动禁用过渡和滚动平滑
- **移动端 padding**：统一为 `px-4 sm:px-6`

### 3. UI 细节打磨
- 菜单改为绝对定位浮层（不占文档流，不推内容）
- 菜单背景半透明（92% opacity + backdrop-blur）
- 自定义滚动条：细窄圆角，适配暗色模式和 Firefox
- 容器宽度统一：Header / Main / Footer 全部 `max-w-5xl mx-auto px-4 sm:px-6`

### 4. Code Review 修复
- 搜索状态加 `aria-live="polite"`（屏幕阅读器可播报结果）
- 菜单 resize 到桌面端时自动重置状态
- 菜单点击外部区域自动关闭

### 5. 文档整理
- 创建 `docs/` 目录，所有项目文档集中管理
- PRD 按版本分目录：`docs/prd/v1.0/PRODUCT_PRD.md`
- 新建 `README.md`（项目主文档）
- 新建 `docs/dev-log/` 开发日志目录

## 问题记录
- ISSUES-005：Pagefind API 误用 `create()`
- ISSUES-006：`__VITE_PRELOAD__` 需用 `is:inline`

## 待办
- M4 任务暂缓：Cloudflare Pages 部署、EdgeOne Makers + DNSPod 分流
- about.ts 数据还是占位，上线前需替换真实信息
